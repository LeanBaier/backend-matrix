server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LeanBaier/backend-matrix.git
          search-paths:
            - Spring/config.server.files